# Task 3: Gold Futures Arbitrage Strategy

## Описание

Арбитражная стратегия между фьючерсами на золото на биржах B3 (Бразилия) и MOEX (Россия).

🔴 **Стратегия убыточна** — широкий спред на B3 (~26 пунктов) делает арбитраж невыгодным.

| Метрика | Значение |
|---------|----------|
| Сделок | 1,590 |
| Win rate | 0.9% |
| Net PnL | -42,052 пунктов |
| Sharpe | -20.84 |
| Margin per trade | $517 |
| ROI on margin | -8,134% |

## Причины убыточности

### Корневая причина: bid-ask spread B3 съедает весь edge

При каждой сделке мы пересекаем спред дважды (вход + выход):

```
Потери на вход:  spread_b3/2 + spread_moex/2 ≈ 13 + 1 = 14 пт
Потери на выход: spread_b3/2 + spread_moex/2 ≈ 13 + 1 = 14 пт
Итого round-trip:                                       ≈ 28 пт
```

Средний PnL на сделку: **-26.45 пт** — практически равен стоимости crossing spread. Mean-reversion сигнал работает (иначе убыток был бы больше), но прибыль от возврата спреда полностью поглощается транзакционными издержками.

### Дополнительные факторы

- **Latency B3** (250 мс) — дополнительный slippage при исполнении
- **Асимметрия ликвидности** — MOEX: узкий спред (2 пт), B3: широкий спред (26 пт)
- **34 минуты данных** — недостаточно для статистически значимой оценки mean-reversion

## Что бы я сделал иначе

1. **Лимитные ордера на B3** — вместо crossing spread выставлять limit order по mid-price. Реализовано и протестировано (см. ниже).
2. **Торговать MOEX, используя B3 как сигнал** — исполняться только на MOEX (спред 2 пт), B3 только для генерации сигналов
3. **Увеличить порог входа до 3-4σ** — входить только при экстремальных отклонениях
4. **Фильтр по ширине спреда B3** — торговать только в моменты высокой ликвидности (spread_b3 < 15 пт)
5. **Больше данных** — 34 минуты недостаточно для оценки стратегии

## Альтернативная стратегия: лимитные ордера

Реализована альтернативная версия бэктеста с limit orders на B3 (`main_limit.py`).

**Идея:** вместо crossing spread — выставляем limit order по mid-price B3. MOEX исполняется market order в момент fill B3 (не в момент сигнала).

### Результаты: Market vs Limit Orders

| Метрика | Market Orders | Limit Orders |
|---------|--------------|-------------|
| Сделок | 1,590 | 17 |
| Net PnL | -42,052 | **-40.63** |
| Avg PnL/сделку | -26.45 | **-2.39** (11x лучше) |
| Win rate | 0.9% | **17.6%** |
| ROI on margin | -8,134% | **-7.9%** |
| Fill rate | 100% | 0.02% |

### Вывод

Лимитные ордера улучшают экономику каждой сделки в 11 раз, но fill rate 0.02% делает стратегию неторгуемой. B3 gold futures слишком неликвидны для обоих подходов.

---

## Быстрый старт

```bash
cd task3-gold-arbitrage
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py        # Оригинальный бэктест (market orders)
python main_limit.py  # Сравнение market vs limit orders
```

## Особенности реализации

### Расчёт спреда по tradeable ценам (не mid-price)

Стратегия использует реальные торгуемые цены (bid/ask), а не теоретические mid-price. По mid-ценам невозможно исполнить сделку.

```python
spread_long = ask_b3 - bid_moex   # цена покупки спреда
spread_short = bid_b3 - ask_moex  # цена продажи спреда
```

Каждый спред имеет свой Z-score (`zscore_long`, `zscore_short`).

### Моделирование latency
- **MOEX**: мгновенное исполнение
- **B3**: задержка 250 мс — исполнение по цене через N тиков после сигнала

### Маржинальные требования
- B3: ~$217 (~1,300 BRL) — [источник](https://clientes.b3.com.br/en/w/launch-of-new-gold-futures-contract)
- MOEX: ~$300 (~30,000 ₽) — [источник](https://smart-lab.ru/q/futures/GDH6/)

## Данные

**Файл:** `quotes_202512260854-GOLD.csv`

| Поле | Описание |
|------|----------|
| ts | Timestamp (наносекунды) |
| symbol | `GLDG26` (B3) или `GOLD-3.26` (MOEX) |
| bid_price | Лучшая цена покупки |
| bid_qty | Объём bid |
| ask_price | Лучшая цена продажи |
| ask_qty | Объём ask |

**Статистика:**
- Период: 2025-11-24 — 2025-12-09
- Строк: 498,188
- B3 avg spread: **26.34** пунктов
- MOEX avg spread: **2.22** пункта

## Конфигурация стратегии

Все параметры стратегии определены в `config.py` через dataclass `StrategyConfig`.

### Торговые инструменты

| Параметр | Значение | Описание |
|----------|----------|----------|
| `symbol_b3` | `GLDG26` | Тикер фьючерса на золото на B3 |
| `symbol_moex` | `GOLD-3.26` | Тикер фьючерса на золото на MOEX |

### Пороги Z-score

| Параметр | Значение | Описание |
|----------|----------|----------|
| `entry_threshold` | 2.0 σ | Вход при отклонении Z-score от среднего |
| `exit_threshold` | 0.5 σ | Выход при возврате Z-score к среднему |
| `stop_loss_threshold` | 4.0 σ | Принудительный выход (стоп-лосс) |
| `zscore_window` | 1000 тиков | Окно rolling-расчёта Z-score |

### Размер позиции

| Параметр | Значение | Описание |
|----------|----------|----------|
| `position_size` | 1 | Количество контрактов на каждой ноге |

### Издержки

| Параметр | Значение | Описание |
|----------|----------|----------|
| `commission_per_contract` | 0.10 BRL | Фиксированная комиссия за контракт |

### Риск-менеджмент

| Параметр | Значение | Описание |
|----------|----------|----------|
| `max_positions` | 1 | Максимум одновременно открытых позиций |
| `min_liquidity` | 1.0 | Минимальный объём на bid/ask для торговли |

### Latency (задержка исполнения)

| Параметр | Значение | Описание |
|----------|----------|----------|
| `b3_latency_ms` | 250 мс | Задержка исполнения на B3 (MOEX = 0 мс) |

### Маржинальные требования (ГО)

| Параметр | Значение | Описание |
|----------|----------|----------|
| `margin_b3` | $217 | ГО на B3 (~1,300 BRL) |
| `margin_moex` | $300 | ГО на MOEX (~30,000 ₽) |

**Итого на сделку:** $517 (B3 + MOEX)

### Пример использования

```python
from config import StrategyConfig, DEFAULT_STRATEGY_CONFIG

# Использовать значения по умолчанию
config = DEFAULT_STRATEGY_CONFIG

# Или создать кастомную конфигурацию
custom_config = StrategyConfig(
    entry_threshold=3.0,  # более консервативный вход
    zscore_window=500,    # более короткое окно
    b3_latency_ms=100,    # оптимистичная задержка
)
```

## Структура проекта

```
task3-gold-arbitrage/
├── config.py              # Параметры стратегии
├── main.py                # Основной скрипт (market orders)
├── main_limit.py          # Сравнение market vs limit orders
├── requirements.txt       # Зависимости
├── strategy.md            # Полная документация
├── src/
│   ├── __init__.py
│   ├── data_loader.py     # Загрузка CSV
│   ├── indicators.py      # Спреды и Z-score
│   ├── backtest.py        # Движок бэктеста (market orders)
│   ├── backtest_limit.py  # Движок бэктеста (limit orders)
│   └── visualization.py   # Plotly графики
└── output/
    ├── backtest_report.md
    ├── comparison_report.md
    ├── backtest_results.png
    ├── trades.csv
    ├── trades_limit.csv
    ├── equity_interactive.html
    └── strategy_dashboard.html
```

## Зависимости

- `pandas` — обработка данных
- `numpy` — расчёты
- `matplotlib` — статические графики
- `plotly` — интерактивные дашборды
