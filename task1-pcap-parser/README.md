# Task 1: B3 PCAP Parser

## Описание

Парсер PCAP-файлов биржи B3 (Brasil, Bolsa, Balcão) для извлечения рыночных данных.

## Цели

1. **Парсинг PCAP → таблицы**
   - Извлечение snapshot (снимки книги заявок)
   - Извлечение updates (инкрементальные обновления)
   - Экспорт в CSV

2. **Календарный спред WDO**
   - Расчёт спреда между фьючерсами WDO разных месяцев экспирации
   - Визуализация спреда

## Статус

🔴 **Ожидание данных** — архив с PCAP-файлами пока не предоставлен

## Структура (планируемая)

```
task1-pcap-parser/
├── src/
│   ├── parser.py          # Основной парсер PCAP
│   ├── snapshot.py        # Обработка snapshot
│   └── updates.py         # Обработка updates
├── output/
│   ├── snapshot.csv
│   └── updates.csv
├── notebooks/
│   └── calendar_spread.ipynb
└── README.md
```

## Зависимости (предварительно)

- `scapy` или `dpkt` — парсинг PCAP
- `pandas` — обработка данных
- `matplotlib` / `plotly` — визуализация
